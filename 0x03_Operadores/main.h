#ifndef HEADER_FILE 
#define HEADER_FILE

float area_circle(float radius);
float Diameter(float radius);
float Circumference(float radius);
float cm_a_m (int cm);
float cm_a_km (int cm);


#endif 